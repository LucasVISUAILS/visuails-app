---
import Layout from '../layouts/Layout.astro';
import SEO from '../components/SEO.astro';
import BreadcrumbSchema from '../components/schema/BreadcrumbSchema.astro';
import FaqSchema from '../components/schema/FaqSchema.astro';
import { getAllServices } from '../lib/content.js';
import { site } from '../data/site.js';

const services = await getAllServices();

const title = 'FAQ — AI Product Visuals for Ecommerce & Fashion Brands';
const description =
  'Answers to common questions about VISUAILS: pricing, turnaround time, worldwide delivery, and how AI-generated catalog, lifestyle, and video visuals work.';

const generalFaqs = [
  {
    q: 'How does pricing work?',
    a: 'Start with a €0.99 test sample (one per business) to see the quality on your own product. After that, pricing is per visual and varies by style — no subscription required. High-volume catalogs and custom campaigns get a tailored quote.',
  },
  {
    q: 'How fast is delivery?',
    a: 'Most orders are delivered within roughly 24 hours. Larger lifestyle or video campaigns may take slightly longer depending on scope.',
  },
  {
    q: 'Which countries do you serve?',
    a: 'Everywhere — delivery is fully digital, so there is no shipping or shoot location involved. We work with ecommerce and fashion brands worldwide.',
  },
  {
    q: "What's the difference between this and stock photography?",
    a: 'Every image is generated specifically for your product using your own product photos as the source — nothing is pulled from a generic stock library, and every result is human-reviewed for accuracy before delivery.',
  },
  {
    q: 'Is there a subscription?',
    a: 'No. VISUAILS is pay-per-visual — you only pay for what you use, starting with a €0.99 sample.',
  },
  {
    q: 'How do I get started?',
    a: `Request your €0.99 test sample, or message us directly on WhatsApp (${site.whatsapp.display}). From there we can scope a full catalog, lifestyle, or video batch.`,
  },
];

const breadcrumbs = [
  { name: 'Home', url: site.url + '/' },
  { name: 'FAQ', url: site.url + '/faq/' },
];
---

<Layout>
  <SEO slot="seo" title={title} description={description} path="/faq/" />
  <BreadcrumbSchema slot="schema" items={breadcrumbs} />
  <FaqSchema slot="schema" faqs={generalFaqs} />

  <nav class="breadcrumbs container" aria-label="Breadcrumb">
    <a href="/">Home</a><span class="sep">/</span><span>FAQ</span>
  </nav>

  <section class="section">
    <div class="container">
      <div class="section-header">
        <h1>Frequently Asked Questions</h1>
      </div>
      <div style="max-width:760px; margin:0 auto 60px;">
        {generalFaqs.map((f) => (
          <details class="faq-item">
            <summary>{f.q}</summary>
            <p>{f.a}</p>
          </details>
        ))}
      </div>

      {services.map((s) => (
        <div style="max-width:760px; margin:0 auto 40px;">
          <h2>{s.name} Questions</h2>
          {s.faqs.map((f) => (
            <details class="faq-item">
              <summary>{f.q}</summary>
              <p>{f.a}</p>
            </details>
          ))}
        </div>
      ))}
    </div>
  </section>

  <section class="section section-alt">
    <div class="container">
      <div class="cta-band">
        <h2>Still have questions?</h2>
        <p>Reach out directly — we typically reply within one business day.</p>
        <div class="hero-actions" style="justify-content:center;">
          <a href="/contact/" class="btn btn-primary">Contact Us</a>
          <a href={site.whatsapp.link} target="_blank" rel="noopener" class="btn btn-whatsapp">WhatsApp</a>
        </div>
      </div>
    </div>
  </section>
</Layout>
